/**
 * For this lab, you will have to write a custom deserializer for a specific object, and well as its corresponding
 * deserializer. Don't worry about conforming to any standard, the tests will simply check if an Object converted to
 * a String can be converted back into its original Object.
 */
public class Lab {
    /**
     * TODO: write a serializing method. (Converting an Object into a String representation)
     */
    public String serializer(CarModel model){
        return null;
    }
    /**
     * TODO: write a deserializing method. (Converting a String representation into an Object)
     */
    public CarModel deserializer(String serialized){
        return null;
    }
}
